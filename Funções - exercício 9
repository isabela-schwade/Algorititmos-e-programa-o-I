/*
5	Crie uma programa que utilizando uma  função em linguagem C que receba 2 números e retorne o maior valor.

%¨&*/


int calcula(int a,int b);
#include<stdio.h>
#include<math.h>
int main(){
    
   int valorA, valorB, op;
   printf("\nDigite um número: ");
   scanf("%d", &valorA);
   
   printf("\nDigite outro número: ");
   scanf("%d", &valorB);
 
   op = calcula(valorA, valorB);
   
   if(op == 1){
       printf("\nO maior número é o número %d", valorA);
   }
   else{
       printf("\nO maior número é o número %d", valorB);
   }
   
}
int calcula(int a,int b){
    
    if(a > b){
        return 1;
    }
}
